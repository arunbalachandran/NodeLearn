// var fs = require('fs')
// fs.writeFileSync('test.txt', 'These are some contents that are temp file worthy')

// var fs = require('fs')
// console.log("The file contents are : " + fs.readFileSync('test.txt').toString());

// var fs = require('path')
var path = require('path')
// give some test path
console.log(path.normalize('test.txt'));